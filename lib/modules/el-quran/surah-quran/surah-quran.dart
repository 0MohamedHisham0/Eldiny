import 'package:flutter/cupertino.dart';

class Surah_Quran extends StatelessWidget {
  const Surah_Quran({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
