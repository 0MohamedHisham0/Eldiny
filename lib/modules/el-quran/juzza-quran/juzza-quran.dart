import 'package:flutter/cupertino.dart';

class Juzz_Quran extends StatelessWidget {
  const Juzz_Quran({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
