import 'package:flutter/cupertino.dart';

class Juzz_Quran_Screen extends StatelessWidget {
  const Juzz_Quran_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
