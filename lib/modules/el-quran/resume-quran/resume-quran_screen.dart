import 'package:flutter/cupertino.dart';

class Resume_Quran_Screen extends StatelessWidget {
  const Resume_Quran_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
