import 'package:flutter/cupertino.dart';

class Resume_Quran extends StatelessWidget {
  const Resume_Quran({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
